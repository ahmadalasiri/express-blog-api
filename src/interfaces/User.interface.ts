export default interface IUser {
  name: string;
  slug: string;
  username: string;
  email: string;
  password: string;
  profilePicture: Object;
  bio: string;
  role: string;
  active: boolean;
}
